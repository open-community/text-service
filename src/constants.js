// ============================================================
// Constants

const ELASTICSEARCH_INDEX = 'open-community';
const DOCUMENT_TYPE = '@open-community/type/text';
const BASE64_REGEXP = /^[a-zA-Z0-9-_]*$/;

// ============================================================
// Exports
export {
    BASE64_REGEXP,
    DOCUMENT_TYPE,
    ELASTICSEARCH_INDEX,
};
